<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Success</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-light">
    <div class="container mt-5">
      <div class="card p-4 shadow-sm mx-auto" style="max-width: 500px">
        <h3 class="mb-3">Welcome, <%= username %>!</h3>
        <p>You have successfully registered.</p>

        <% if (error) { %>
        <div class="alert alert-danger mt-2"><%= error %></div>
        <% } else if (success) { %>
        <div class="alert alert-success mt-2"><%= success %></div>
        <% } %>

        <hr />

        <!-- Update Full Name Form -->
        <form action="/update-fullname" method="POST">
          <div class="mb-3">
            <label for="new_fullname" class="form-label"
              >Change your full name:</label
            >
            <input
              type="text"
              class="form-control"
              name="new_fullname"
              id="new_fullname"
              required
            />
            <input type="hidden" name="old_fullname" value="<%= username %>" />
          </div>
          <div class="row">
            <div class="col d-grid">
              <button type="submit" class="btn btn-warning">
                Update Full Name
              </button>
            </div>
          </div>
        </form>

        <!-- Logout Form -->
        <form action="/logout" method="POST" class="mt-2">
          <div class="row">
            <div class="col d-grid">
              <button type="submit" class="btn btn-outline-secondary">
                Logout
              </button>
            </div>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
